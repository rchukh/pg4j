description = 'pg4j support for spring-data-jpa'

// Properties
ext {
	spring_data_jpa_version = "1.2.0.RELEASE"
}

dependencies {
	compile project(':pg4j-core'),
	compile(
		[group: 'org.springframework.data', name:'spring-data-jpa', version:"${spring_data_jpa_version}"]
	)
}